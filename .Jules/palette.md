## 2026-04-11 - Missing ARIA Labels on Icon-Only Buttons
**Learning:** Found multiple instances where shadcn `<Button size="icon">` components are missing `aria-label`s or Tooltip text for screen readers.
**Action:** Ensure icon-only buttons include `aria-label` for accessibility.
