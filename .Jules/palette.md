## 2026-05-20 - Add aria-labels to icon-only buttons
**Learning:** Found multiple instances where `size="icon"` buttons in shadcn/ui components omit `aria-label` attributes. This hinders screen reader accessibility.
**Action:** Adding `aria-label` attributes to these components to ensure clear intent for screen reader users.
